<%= form_with(model: menu_item, local: true) do |form| %>
  <% if menu_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(menu_item.errors.count, "error") %> prohibited this menu_item from being saved:</h2>

      <ul>
        <% menu_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag 'current_menu_id', @current_menu_id%>

  <div style="visibility: hidden;">
    <%= form.text_field :menu_id %>
  </div>

  <div class="Form-section">
    <%= form.label :menu_item_name ,class:"FormInput-label" %>
    <%= form.text_field :menu_item_name , class: "Form-input" %>
  </div>

  <div class="Form-section">
    <%= form.label :menu_item_price ,class:"FormInput-label" %>
    <%= form.text_field :menu_item_price, class: "Form-input" %>
  </div>

  <div class="Form-section">
    <%= form.label :image_url ,class:"FormInput-label" %>
    <%= form.text_field :image_url, class: "Form-input" %>
  </div>

  <div class="Form-section">
    <%= form.submit nil, class: "Form-submit" %>
  </div>
<% end %>
